<Page x:Class="TestovoeNabiullinVladislav.View.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestovoeNabiullinVladislav.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="1">
            <StackPanel Margin="6">
                <Label Content="Поиск"/>
                <TextBox/>
                <Button Content="Добавить Клиента" Margin="6" Command="{Binding AddUserCommand}"/>
            </StackPanel>
        </Grid>

        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя пользователя"/>
                <DataGridTextColumn Header="Баланс"/>
                <DataGridTextColumn Header="Кошелёк"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Grid.Row="1">
      
        </DataGrid>

    </Grid>
</Page>
